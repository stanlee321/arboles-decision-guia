# Datos procesados

Esta carpeta queda reservada para transformaciones futuras. En este ejemplo el
CSV de entrada ya tiene una forma tabular apta para el modelado, por lo que el
pipeline lo lee directamente desde `data/raw/`.
